/*import './AnnouncementBar.css';

const messages = [
  '✈️ ENVÍOS A TODO EL PAÍS',
  '💳 3 CUOTAS SIN INTERÉS',
  '🏦 10% OFF TRANSFERENCIA BANCARIA',
];

export default function AnnouncementBar() {
  // Duplicate messages to create seamless loop
  const track = [...messages, ...messages, ...messages, ...messages];

  return (
    <div className="ph-announcement" aria-label="Promociones">
      <div className="ph-announcementTrack">
        {track.map((msg, i) => (
          <span key={i} className="ph-announcementItem">{msg}</span>
        ))}
      </div>
    </div>
  );
}
*/