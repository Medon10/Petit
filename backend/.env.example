# Backend environment example
# Copy to backend/.env and fill with your real values

# --- App ---
NODE_ENV=development
JWT_SECRET=change_me
JWT_EXPIRES_IN=12h
FRONTEND_ORIGINS=http://localhost:5173

# --- Database (MySQL) ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=petit
DB_TIMEZONE=local

# Alternative: single connection string (recommended for cloud DBs)
# Example:
# DB_URL=mysql://user:pass@host:3306/petit
# DB_URL=

# Local MySQL usually doesn't require TLS
DB_SSL=false
DB_SSL_REJECT_UNAUTHORIZED=true

# --- Email notifications ---
# Enable/disable sending
MAIL_ENABLED=true

# Who receives the notifications (boss email). For testing you can put your own email here.
MAIL_TO=tu_mail_de_prueba@gmail.com

# From address (must be verified if using SendGrid Single Sender, or match SMTP_USER for Gmail)
MAIL_FROM=tu_mail_de_prueba@gmail.com

# Option 1: SendGrid API (recommended when you can't access DNS)
# - Create API key in SendGrid
# - Verify a Single Sender (email verification link) for the address you will use in MAIL_FROM
# Docs: https://docs.sendgrid.com/ui/sending-email/sender-verification
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Option 2: SMTP (Gmail or custom)
# If you use Gmail, use an App Password (not your normal password)
# Note: Some hosting providers (like Render) may block SMTP ports
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu_mail_de_prueba@gmail.com
SMTP_PASS=app_password_aqui
